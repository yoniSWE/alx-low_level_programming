#include <stdio.h>
/**
 * main - entry point
 *
 * Description: Prints all single digit numbers starting from 0
 * Return: 0
 */
int main(void)
{
	int b;

	for (b = 0; b < 10; b++)
		printf("%d\n", b);

	return (0);
}
