#include <stdio.h>
/**
 * main - entry point
 *
 * Description: Prints all single digit numbers starting from 0
 * Return: 0
 */
int main(void)
{
	int b;

	for (c = 0; c < 10; c++)
		printf("%d", c);

	return (0);
}
